package methods;

public class VoidVSReturnMethods {
    public static void main(String[] args) {
        /*
        RETURN TYPE --- max(), min()
        VOID TYPE ----- print(), println()
        if you want byte max=Math.max(5,10) need to cast.
         */
        int max=Math.max(5,10);

        System.out.println();
    }
}
